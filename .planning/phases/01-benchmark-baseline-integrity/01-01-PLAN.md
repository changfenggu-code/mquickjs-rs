---
phase: 01-benchmark-baseline-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/canary_benchmarks.txt
  - benches/compare.sh
  - .github/workflows/bench.yml
  - tests/benchmark_workflow_tests.rs
autonomous: true
requirements:
  - BNCH-01
must_haves:
  truths:
    - "Maintainer has one canonical local Rust-vs-C comparison flow that targets the checked-in contrib/mquickjs reference tree."
    - "CI uses the same canary set and contrib/mquickjs lookup assumptions as the local comparison flow."
    - "Benchmark-plumbing drift is caught by automated verification instead of being rediscovered during later optimization work."
  artifacts:
    - path: "benches/canary_benchmarks.txt"
      provides: "Shared canonical benchmark manifest for local scripts, CI, and later docs."
      contains: "method_chain"
    - path: "benches/compare.sh"
      provides: "Canonical Rust-vs-C benchmark driver wired to contrib/mquickjs and the shared canary manifest."
      contains: "contrib/mquickjs"
    - path: ".github/workflows/bench.yml"
      provides: "CI benchmark workflow aligned to the same manifest and reference-tree assumptions."
      contains: "contrib/mquickjs"
    - path: "tests/benchmark_workflow_tests.rs"
      provides: "Regression coverage for benchmark manifest/path consistency."
      contains: "canary_benchmarks"
  key_links:
    - from: "benches/compare.sh"
      to: "benches/canary_benchmarks.txt"
      via: "shared canary manifest parsing"
      pattern: "canary_benchmarks"
    - from: ".github/workflows/bench.yml"
      to: "benches/canary_benchmarks.txt"
      via: "workflow shell step reading shared manifest"
      pattern: "canary_benchmarks"
    - from: "tests/benchmark_workflow_tests.rs"
      to: "benches/compare.sh and .github/workflows/bench.yml"
      via: "file-content assertions"
      pattern: "contrib/mquickjs"
---

<objective>
Lock benchmark plumbing to one trustworthy baseline so later optimization work is measuring the same Rust-vs-C comparison locally and in CI.

Purpose: close the remaining drift risk around benchmark reference paths, canary selection, and silent fallback behavior.
Output: a shared canary manifest, aligned local/CI benchmark runners, and regression coverage that fails when those assumptions diverge.
</objective>

<execution_context>
@C:/Users/27816/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/27816/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@benches/compare.sh
@.github/workflows/bench.yml
@benches/js_benchmarks.rs
@docs/BENCHMARK_ANALYSIS.md

<interfaces>
The new shared manifest in `benches/canary_benchmarks.txt` is the contract for benchmark-plumbing alignment.

Required format:
- one non-comment entry per line
- each line maps `script_name|criterion_benchmark_name`
- Phase 1 canonical entries must cover `method_chain`, `runtime_string_pressure`, `for_of_array`, and `deep_property`

Downstream consumers:
- `benches/compare.sh` reads the script-name side to decide which JS files are canonical
- `.github/workflows/bench.yml` reads the same file for the GitHub summary table
- later docs/helper scripts must cite this manifest instead of hardcoding a second canary list
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical benchmark manifest and make local comparison use it</name>
  <read_first>
@AGENTS.md
@benches/compare.sh
@benches/js_benchmarks.rs
@docs/BENCHMARK_ANALYSIS.md
  </read_first>
  <files>benches/canary_benchmarks.txt, benches/compare.sh</files>
  <acceptance_criteria>
- `benches/canary_benchmarks.txt` exists and lists exactly the four phase canaries with script-to-Criterion mappings.
- `benches/compare.sh` reads the shared manifest for its default canonical run instead of hardcoding or globbing the whole scripts directory.
- The canonical Rust-vs-C path resolves only through `contrib/mquickjs` and errors with a clear message when the C reference binary/source is unavailable, unless an explicit non-canonical fallback flag is provided.
  </acceptance_criteria>
  <action>Create `benches/canary_benchmarks.txt` as the single source of truth for Phase 1 canaries, with one `script_name|criterion_benchmark_name` entry per line for `method_chain`, `runtime_string_pressure`, `for_of_array`, and `deep_property` (per BNCH-02 scope). Update `benches/compare.sh` to parse that manifest, treat it as the default canonical Rust-vs-C flow for benchmark integrity work, keep any broader all-script sweep behind an explicit opt-in flag such as `--all`, and hard-stop with actionable guidance if `contrib/mquickjs` is missing so the canonical flow cannot silently degrade into undocumented Rust-only behavior.</action>
  <verify>
    <automated>bash -n benches/compare.sh</automated>
  </verify>
  <done>The local comparison driver and shared manifest express one canonical canary comparison flow rooted in `contrib/mquickjs`, with non-canonical modes clearly separated.</done>
</task>

<task type="auto">
  <name>Task 2: Align the CI benchmark workflow to the same manifest and reference assumptions</name>
  <read_first>
@.github/workflows/bench.yml
@benches/compare.sh
@benches/canary_benchmarks.txt
@AGENTS.md
  </read_first>
  <files>.github/workflows/bench.yml</files>
  <acceptance_criteria>
- `.github/workflows/bench.yml` reads `benches/canary_benchmarks.txt` for the Rust-vs-C canary table instead of maintaining its own hardcoded benchmark list.
- CI uses the same `contrib/mquickjs` lookup/build logic as the canonical local flow.
- The workflow fails loudly when the checked-in C reference tree cannot satisfy the canonical Rust-vs-C comparison assumptions.
  </acceptance_criteria>
  <action>Refactor `.github/workflows/bench.yml` so the benchmark-summary job derives its canonical Rust-vs-C comparison rows from `benches/canary_benchmarks.txt`, keeps the startup baseline row, and uses the same `contrib/mquickjs` discovery/build expectations as `benches/compare.sh`. Remove any silent fallback that would make CI appear canonical while actually skipping the reference-tree comparison, and keep Criterion analysis as the separate Rust-only table described in the phase goal.</action>
  <verify>
    <automated>rg "canary_benchmarks|contrib/mquickjs" .github/workflows/bench.yml</automated>
  </verify>
  <done>CI and the local comparison script are anchored to the same canary manifest and the same reference-tree assumptions, so the canonical benchmark path is mechanically consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for benchmark-plumbing consistency</name>
  <read_first>
@tests
@benches/canary_benchmarks.txt
@benches/compare.sh
@.github/workflows/bench.yml
@AGENTS.md
  </read_first>
  <files>tests/benchmark_workflow_tests.rs</files>
  <acceptance_criteria>
- A new integration-style test reads the manifest, local script, and CI workflow from disk and asserts the canonical canary names are present in one place and referenced from the other two.
- The test asserts `contrib/mquickjs` is the live reference-tree path in both benchmark runners.
- The test is specific enough to fail if a future edit reintroduces independent hardcoded canary lists or an old reference-tree location.
  </acceptance_criteria>
  <action>Add `tests/benchmark_workflow_tests.rs` that performs file-content regression checks for the benchmark manifest and runner wiring rather than engine runtime behavior. The test should parse `benches/canary_benchmarks.txt`, assert the four required canaries are present, assert `benches/compare.sh` and `.github/workflows/bench.yml` both reference that manifest, and assert both files use `contrib/mquickjs` rather than any stale alternate path. Keep the test fast and deterministic so it can run as the lightweight automation guard for Phase 1 plumbing.</action>
  <verify>
    <automated>cargo test -p mquickjs-rs --test benchmark_workflow_tests</automated>
  </verify>
  <done>Benchmark-plumbing integrity is protected by an automated test that fails before later optimization work can proceed on drifted assumptions.</done>
</task>

</tasks>

<verification>
Run the new benchmark-plumbing regression test and confirm the local comparison script is shell-valid:

<automated>cargo test -p mquickjs-rs --test benchmark_workflow_tests</automated>
<automated>bash -n benches/compare.sh</automated>
</verification>

<success_criteria>
The repository has one shared canary manifest, both local and CI Rust-vs-C flows consume it, and automated coverage fails if either runner drifts away from `contrib/mquickjs` or the canonical canary set.
</success_criteria>

<output>
After completion, create `.planning/phases/01-benchmark-baseline-integrity/01-01-SUMMARY.md`
</output>
