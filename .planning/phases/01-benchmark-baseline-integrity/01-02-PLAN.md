---
phase: 01-benchmark-baseline-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - benches/run_canaries.sh
  - docs/BENCHMARK_ANALYSIS.md
  - docs/BENCHMARK_ANALYSIS.zh.md
autonomous: true
requirements:
  - BNCH-01
  - BNCH-02
must_haves:
  truths:
    - "Maintainer can rerun `method_chain`, `runtime_string_pressure`, `for_of_array`, and `deep_property` with one documented helper flow after hotspot changes."
    - "English and Chinese benchmark docs describe the same canonical canary workflow and reference-tree assumptions."
    - "Benchmark guidance clearly separates Criterion analysis from Rust-vs-C comparison so numbers are interpreted consistently."
  artifacts:
    - path: "benches/run_canaries.sh"
      provides: "Maintainer-facing canary rerun helper driven by the shared manifest."
      contains: "canary_benchmarks"
    - path: "docs/BENCHMARK_ANALYSIS.md"
      provides: "English benchmark workflow and canary documentation aligned to the live scripts/CI."
      contains: "method_chain"
    - path: "docs/BENCHMARK_ANALYSIS.zh.md"
      provides: "UTF-8 Chinese benchmark workflow and canary documentation aligned to the English source."
      contains: "method_chain"
  key_links:
    - from: "benches/run_canaries.sh"
      to: "benches/canary_benchmarks.txt"
      via: "shared manifest parsing"
      pattern: "canary_benchmarks"
    - from: "docs/BENCHMARK_ANALYSIS.md"
      to: "benches/run_canaries.sh"
      via: "documented canonical rerun command"
      pattern: "run_canaries"
    - from: "docs/BENCHMARK_ANALYSIS.zh.md"
      to: "docs/BENCHMARK_ANALYSIS.md"
      via: "synchronized workflow sections"
      pattern: "runtime_string_pressure"
---

<objective>
Make the canonical canaries easy to rerun and document the benchmark workflow so maintainers use the same commands and interpretation rules after every hotspot change.

Purpose: turn the newly aligned benchmark plumbing into an explicit maintainer workflow instead of tribal knowledge.
Output: a canary rerun helper plus synchronized English/Chinese benchmark docs that point to the same live commands and assumptions.
</objective>

<execution_context>
@C:/Users/27816/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/27816/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@benches/canary_benchmarks.txt
@benches/compare.sh
@benches/js_benchmarks.rs
@docs/BENCHMARK_ANALYSIS.md
@docs/BENCHMARK_ANALYSIS.zh.md

<interfaces>
Shared inputs already created by Plan `01-01`:

From `benches/canary_benchmarks.txt`:
- `method_chain|method_chain 5k`
- `runtime_string_pressure|runtime_string_pressure 4k`
- `for_of_array|for_of_array 20k`
- `deep_property|deep_property 200k`

The helper and docs in this plan must cite those names instead of inventing alternate labels or benchmark subsets.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a maintainer helper for canonical canary reruns</name>
  <read_first>
@benches/canary_benchmarks.txt
@benches/compare.sh
@benches/js_benchmarks.rs
@AGENTS.md
  </read_first>
  <files>benches/run_canaries.sh</files>
  <acceptance_criteria>
- `benches/run_canaries.sh` reads `benches/canary_benchmarks.txt` instead of hardcoding a second benchmark list.
- The script exposes a lightweight introspection mode such as `--list` so maintainers can confirm the canonical set without running long benchmarks.
- The script provides explicit commands for Criterion-only reruns and for the canonical Rust-vs-C comparison path introduced in Plan `01-01`.
  </acceptance_criteria>
  <action>Create `benches/run_canaries.sh` as the maintainer entry point for Phase 1 canaries. It should parse `benches/canary_benchmarks.txt`, support a `--list` mode, support a Criterion-focused rerun path that invokes only the four canonical benchmark names from `benches/js_benchmarks.rs`, and provide an explicit way to delegate to the canonical Rust-vs-C comparison flow from `benches/compare.sh`. Keep the helper shell-only and narrow: it exists to make the canary rerun workflow easy and consistent, not to add a new benchmark framework.</action>
  <verify>
    <automated>bash benches/run_canaries.sh --list</automated>
  </verify>
  <done>The canonical canary set can be enumerated and rerun from one helper that derives its targets from the shared manifest rather than duplicated hardcoded names.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the benchmark workflow docs around the live canary flow</name>
  <read_first>
@docs/BENCHMARK_ANALYSIS.md
@docs/BENCHMARK_ANALYSIS.zh.md
@benches/run_canaries.sh
@benches/compare.sh
@.github/workflows/bench.yml
@AGENTS.md
  </read_first>
  <files>docs/BENCHMARK_ANALYSIS.md, docs/BENCHMARK_ANALYSIS.zh.md</files>
  <acceptance_criteria>
- The English doc names the canonical canary set, the helper command, the local Rust-vs-C command, and the CI benchmark summary behavior from the live files.
- The Chinese doc mirrors the same workflow changes and is saved as valid UTF-8 instead of mojibake.
- The updated docs stop describing benchmark roles in a way that conflicts with the shared manifest, helper script, or CI workflow.
  </acceptance_criteria>
  <action>Update `docs/BENCHMARK_ANALYSIS.md` so its workflow sections describe the live Phase 1 benchmark process: shared canary manifest, canonical helper command, separate Criterion-versus-Rust-vs-C roles, and `contrib/mquickjs` as the checked-in reference tree. Rewrite the matching sections of `docs/BENCHMARK_ANALYSIS.zh.md` to keep the Chinese pair synchronized and repair the file to valid UTF-8 while preserving the same meaning. Keep the docs tightly scoped to benchmark baseline integrity; do not broaden this plan into optimization claims or GC/runtime architecture work.</action>
  <verify>
    <automated>rg "run_canaries|contrib/mquickjs|method_chain|runtime_string_pressure|for_of_array|deep_property" docs/BENCHMARK_ANALYSIS.md docs/BENCHMARK_ANALYSIS.zh.md</automated>
  </verify>
  <done>Both benchmark analysis docs point maintainers at the same live canary workflow and no longer disagree with the scripts or CI benchmark path.</done>
</task>

</tasks>

<verification>
Confirm the helper enumerates the canonical canaries and the docs cite the live workflow:

<automated>bash benches/run_canaries.sh --list</automated>
<automated>rg "run_canaries|contrib/mquickjs|method_chain|runtime_string_pressure|for_of_array|deep_property" docs/BENCHMARK_ANALYSIS.md docs/BENCHMARK_ANALYSIS.zh.md</automated>
</verification>

<success_criteria>
Maintainers can rerun the four Phase 1 canaries from one helper, and both benchmark-analysis documents describe the same canonical local/CI workflow without path or encoding drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-benchmark-baseline-integrity/01-02-SUMMARY.md`
</output>
